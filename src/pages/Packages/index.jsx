import { ScrollButton } from "../../components";
import { PackagesWrapper } from "../../features";

export const Packages = () => {
  return (
    <>
      <ScrollButton />
      <PackagesWrapper />
    </>
  );
};
export default Packages;
